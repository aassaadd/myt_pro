'use strict';

/**
 * @ngdoc function
 * @name mytPcHtmlApp.controller:ProductauditmessageCtrl
 * @description
 * # ProductauditmessageCtrl
 * Controller of the mytPcHtmlApp
 */
angular.module('mytPcHtmlApp')
  .controller('ProductauditmanageCtrl', function (global) {
      global.currentMenu={
        no:'productAuditMessage',
        name:'产品审核管理',
        ico:'',
        path:'productAuditMessage'
      }
  });
