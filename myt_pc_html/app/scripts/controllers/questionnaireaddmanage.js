'use strict';

/**
 * @ngdoc function
 * @name mytPcHtmlApp.controller:QuestionnaireaddmanageCtrl
 * @description
 * # QuestionnaireaddmanageCtrl
 * Controller of the mytPcHtmlApp
 */
angular.module('mytPcHtmlApp')
  .controller('QuestionnaireaddmanageCtrl', function (global) {
    global.currentMenu={
      no:'questionnaireMessage',
      name:'问卷管理',
      ico:'',
      path:'questionnaireMessage'
    }
  });
