'use strict';

/**
 * @ngdoc function
 * @name mytPcHtmlApp.controller:ProductapplymanageCtrl
 * @description
 * # ProductapplymanageCtrl
 * Controller of the mytPcHtmlApp
 */
angular.module('mytPcHtmlApp')
  .controller('ProductapplymanageCtrl', function (global) {
    global.currentMenu={
      no:'productMessage',
      name:'产品管理',
      ico:'',
      path:'productMessage'
    }
  });
